name: velour_app
description: >-
  Velour — puzzle néon par runs : formes, couleurs, perfect, chrono,
  mises LUX (casual / high stakes / royal), forge, thèmes néon et
  classement en ligne (Firebase).
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
# Même numéro après « + » : Android versionCode et iOS CFBundleVersion (incrémenter à chaque upload store).
version: 1.2.3+21

environment:
  sdk: ^3.11.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.20.2

  provider: ^6.1.1
  google_fonts: ^6.2.1
  shared_preferences: ^2.2.3
  audioplayers: ^6.1.0
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  package_info_plus: ^10.1.0
  cloud_functions: ^6.2.0
  in_app_purchase: ^3.2.3
  firebase_crashlytics: ^5.2.0
  audio_session: ^0.2.3
  firebase_app_check: ^0.4.3
  firebase_remote_config: ^6.1.1
  url_launcher: ^6.3.1
  firebase_analytics: ^12.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  in_app_purchase_platform_interface: ^1.4.0
  plugin_platform_interface: ^2.1.8

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  fake_cloud_firestore: ^4.1.0+1
  firebase_core_platform_interface: ^6.0.3
  flutter_launcher_icons: ^0.14.4
  fake_async: ^1.3.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/audio/music_main.mp3
    - assets/audio/sfx_match.mp3
    - assets/audio/sfx_perfect.mp3
    - assets/audio/sfx_click.mp3
    - assets/audio/sfx_tap.mp3
    - assets/audio/sfx_level_up.mp3
    - assets/audio/sfx_credit.mp3

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Icônes Android : activer le mode **adaptive** (cercle / squircle / carré arrondi).
# Sans foreground+background, seuls des PNG mipmaps « plein cadre » masquent mal sur
# certains launchers — critère fort pour les vitrines type « Choix de l’équipe » Play.
#
# iOS / App Store (Apple) : image **carrée** ; le système applique le masque « squircle »
# (ne pas pré-arrondir les coins). L’icône marketing 1024×1024 ne doit pas comporter de
# transparence — voir « Configuring your app icon » + HIG App icons sur developer.apple.com.
# Variantes iOS 18+ (Dark / Tinted) : assets dérivés de `app_icon.png` (voir scripts si besoin).
flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/icons/app_icon.png
  adaptive_icon_foreground: assets/icons/app_icon.png
  # Couleur de fond derrière le foreground (coins visibles sous masque circulaire).
  # Or chaud proche du fond texturé de l’icône Velour pour limiter l’effet de découpe.
  adaptive_icon_background: "#C9A03A"
  # App Store : pas de canal alpha sur les PNG d’icône — fusion sur fond proche du gold Velour.
  remove_alpha_ios: true
  background_color_ios: "#C9A03A"
  # Dark : fond transparent (système fournit le fond). Tinted : niveaux de gris.
  image_path_ios_dark_transparent: assets/icons/app_icon_ios_dark.png
  image_path_ios_tinted_grayscale: assets/icons/app_icon_ios_tinted.png
  desaturate_tinted_to_grayscale_ios: true
